1. Display "Positive Number: " to prompt the user input a positive integer between 1 and 8 and store it in n.
2. For each row from 1 to 'n':
   - Print 'n - the current row number' spaces.
   - Print 'the current row number' hash symbols ('#') for the pyramid.
   - Move to the next line for the next row.
